{
  "name": "wa-bot-lokal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "node --env-file=.env dist/index.js",
    "test": "pnpm run build && node --test dist/app.test.js"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^7.0.2"
  }
}
